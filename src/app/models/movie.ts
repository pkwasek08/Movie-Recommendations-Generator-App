export class Movie {
    id: number;
    index: number;
    title: string;

    constructor(id: number, index: number, name: string) {
        this.id = id;
        this.index = index;
        this.title = name;
    }
}
